import 'package:flutter/material.dart';

class OracoesPage extends StatelessWidget {
  const OracoesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Orações')),
      body: const Center(child: Text('Conteúdo da página Orações')),
    );
  }
}
